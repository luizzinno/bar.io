import React from 'react';
import Typography from '@material-ui/core/Typography';

const Custom404Page: React.FunctionComponent = (props) => {
  return <Typography>404 Page</Typography>;
};

export default Custom404Page;
