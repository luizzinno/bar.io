import { css } from 'emotion';
import { theme } from 'core/theme';

export const icon = css`
  font-size: 5rem;
`;

export const iconDrawer = css`
  flex: 0 0 1.5rem;
  width: 1.5rem;
  font-size: 1.5rem;
`;

export const title = css`
  margin-top: 1.5rem;
  font-size: 1.25rem;
  text-align: center;
`;

export const titleDrawer = css`
  flex: 1 1 auto;
  width: auto;
  margin-top: 0;
  padding-left: 0.5rem;
  font-size: 1.125rem;
  text-align: left;
`;

export const subtitle = css`
  margin-top: 1.25rem;
`;

export const subtitleDrawer = css`
  display: none;
`;
